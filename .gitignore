# ─── .gitignore ──────────────────────────────────────────────────────
# Environment variables (NEVER commit these)
.env
.env.local

# Posted history (runtime state — not needed in git)
posted.json

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db
